# src/ — module index
#
# ingest_stats19.py   — load STATS19 collision / vehicle / casualty CSVs  ✅ done
# ingest_aadf.py      — load DfT AADF by-direction CSV                    🔜 next
# ingest_webtris.py   — WebTRIS API client for MIDAS sensor data           🔜
# ingest_mrdb.py      — load MRDB shapefile                               🔜
# clean.py            — per-source cleaning and validation                 🔜
# join.py             — spatial + attribute joins across sources           🔜
# features.py         — feature engineering, COVID flag, encoding          🔜
# model.py            — train / evaluate / explain model                   🔜
# config.py           — central config loader                              ✅ done
